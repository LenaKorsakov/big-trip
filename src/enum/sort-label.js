import Enum from './enum';

export default class SortLabel extends Enum {
  static DAY = 'Day';
  static EVENT = 'Event';
  static TIME = 'Time';
  static PRICE = 'Price';
  static OFFERS = 'Offers';
}
